version: '3.8'

services:
  app-beasiswa:
    build:
      context: .
      dockerfile: Dockerfile
    image: beasiswa-app
    container_name: beasiswa_app
    restart: unless-stopped
    environment:
      - APP_ENV=production
      - APP_DEBUG=false
      - DB_CONNECTION=pgsql
      - DB_HOST=db-beasiswa          # Menunjuk ke service 'db-beasiswa'
      - DB_PORT=5432
      - DB_DATABASE=${DB_DATABASE}
      - DB_USERNAME=${DB_USERNAME}
      - DB_PASSWORD=${DB_PASSWORD}
      - SSO_JWKS_URL=${SSO_JWKS_URL}
    depends_on:
      - db-beasiswa
    networks:
      - beasiswa_network
      - proxy-tier

  db-beasiswa:
    image: postgres:16-alpine
    container_name: beasiswa_db
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_DB=${DB_DATABASE}
      - POSTGRES_USER=${DB_USERNAME}
      - POSTGRES_PASSWORD=${DB_PASSWORD}
    volumes:
      - beasiswa_db_data:/var/lib/postgresql/data
    networks:
      - beasiswa_network

volumes:
  beasiswa_db_data:

networks:
  beasiswa_network:
    driver: bridge
  proxy-tier:
    external: true